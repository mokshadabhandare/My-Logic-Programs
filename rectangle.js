//Program to calculate the area of rectangle
//length*width

let length=parseInt(prompt("Enter length"));
let width=parseInt(prompt("Enter width"));

document.write("Area of rectangle","<br>"+length*width)